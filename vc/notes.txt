base VC for lei


// signature type (Sam to confirm year, i.e. 2018 or different)

// date of creation of signature (Sam to provide anchor to KEL part)

// Need issuanceDate to future proof indy
// Need indy array properties
// Indy tooling creates and registers a schema but its just an array
// Future proof for JSON-LD is to define @context not JSON schema
//official vc vocabulary
// https://github.com/w3c/vc-data-model/blob/main/vocab/credentials.jsonld
// credentialSubject.id
// credentialSubject.lei
// 
